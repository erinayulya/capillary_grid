АРХИТЕКТУРА ПРОЕКТА
Дата анализа: 16.09.2026. Описаны текущие рабочие .m-файлы проекта.
Структура папок реализована 16.09.2026. Формулы перенесённых функций сохранены.

1. ТЕКУЩЕЕ УСТРОЙСТВО

Рабочие файлы разделены по ответственности:
- run.m: графический ввод параметров, проверка ввода, запуск.
- main.m: параметры, переключатель recordResults, инициализация состояния.
- model/: физическая модель, обновление состояния и цикл simulateNetwork.
- solver/: 3 файла численного решения.
- visualization/: drawNetwork.m и plotNetwork.m для отрисовки.
- reporting/: создание PDF по сохранённым результатам.
- results/: отдельная папка каждого запуска с PDF и MAT.
- setupProject.m: подключение рабочих папок.

Основная структура данных — Net. Большинство расчётных функций получает
Net и возвращает обновлённый Net. Разделение вычислений на функции уже есть;
глубокая иерархия папок для такого количества файлов не нужна.

2. РЕАЛИЗОВАННАЯ СТРУКТУРА

capillary_grid/
  setupProject.m             подключение рабочих папок
  main.m                     точка запуска расчёта
  run.m                      текущая точка запуска интерфейса
  model/
    simulateNetwork.m
    calcCanMove.m
    calcRegime.m
    calcSaturation.m
    calcState.m
    calcTimeStep.m
    calcWaterConnectivity.m
    capillaryEquation.m
  solver/
    solvePressureFlow.m
    calcResidualJacobian.m
    pressureFlowScaling.m
  visualization/
    drawNetwork.m
    plotNetwork.m
  reporting/
    writeSimulationReport.m
  results/                   сохранённые запуски (PDF и MAT)
  docs/
    architecture.txt
    functions.txt
    variables.txt
    maintenance.txt
    start.txt
  AGENTS.md                  правила для работы помощника с проектом

Это один уровень вложенности по ответственности. Локальные функции
calcOneRegime, processNode и подобные разумно оставить в их .m-файлах:
они обслуживают конкретную основную функцию.

setupProject подключает корень, model, solver, visualization, reporting относительно
собственного файла. main и run вызывают его автоматически. Для прямого
вызова отдельных функций сначала выполнить setupProject из корня проекта.
3. СВЯЗИ

run -> plotNetwork (предпросмотр)
run -> main (через assignin/evalin и базовое рабочее пространство)
main -> simulateNetwork -> solvePressureFlow -> pressureFlowScaling
                         -> calcResidualJacobian -> capillaryEquation
simulateNetwork -> calcCanMove -> calcWaterConnectivity
simulateNetwork -> calcRegime
simulateNetwork -> calcTimeStep
simulateNetwork -> calcSaturation -> calcState
simulateNetwork -> drawNetwork (интерактивный режим)
simulateNetwork -> MAT -> writeSimulationReport -> drawNetwork (запись)

Первый расчёт в simulateNetwork:
solvePressureFlow -> calcTimeStep -> drawNetwork.

Один шаг цикла после сохранения предыдущего состояния:
calcSaturation -> calcState -> calcCanMove -> calcRegime
-> solvePressureFlow -> calcTimeStep -> drawNetwork.
Порядок расчётных функций сохранён. В режиме записи вместо экранной отрисовки
сохраняется снимок MAT. После завершения собирается PDF с итогом на первом листе.

4. ЧТО ВАЖНЕЕ ПЕРЕНОСА ПО ПАПКАМ

А. Передача параметров run -> main исправлена: main больше не удаляет Net.
При отсутствии Net.V.P0 задаётся 200 Па. В интерфейсе отдельного поля для
верхнего давления пока нет. Цикл вынесен в simulateNetwork(Net,...).
Повторный main использует существующие параметры Net; для исходных настроек
нужно сначала выполнить clear Net.

Г. Имя run совпадает с именем стандартной функции MATLAB.
При согласованной переработке интерфейс лучше назвать runCapillaryApp.

Д. Нет явного успешного/неуспешного статуса решателя.
solvePressureFlow при превышении допуска после итераций предупреждает,
но возвращает результат. Это ограничение самого решателя сохраняется.
Автоматический режим останавливается при неконечном/неположительном dt,
прорыве, ошибке или достижении лимита шагов. Частичный результат помечается
причиной завершения; для интерактивного режима сохраняются диалоги.

Е. Формулы Pc, Aeff и B, а также выбор соседних узлов повторяются.
При развитии модели полезно выделить общие вычисления и соглашения об
индексации. Сейчас сами папки не устранят это дублирование.

Ж. Комментарий к VerticalBC=false исправлен: границы закрыты.

5. ГРАНИЦЫ АНАЛИЗА

При переносе проверены хеши всех 12 перемещённых файлов: содержимое сохранено.
В отдельном процессе MATLAB R2025b проверены пути из другого текущего каталога,
однофазная сеть 2x2 (давления 1000/500 Па при входных 1500 Па, ожидаемые
горизонтальные расходы и нулевые вертикальные), drawNetwork, plotNetwork
и создание окна run. Проверки прошли.
Для режима записи выполнен полный расчёт текущих параметров main: 11 шагов,
сумма dt = 3.24882469854 с, достижение правой границы. Сумма сверена с MAT.
PDF содержит 13 листов: титульный, шаг 0, шаги 1-11. Проверены подписи всех
13 матриц на каждом листе шага и визуальное размещение элементов.
Это проверка организации записи, не независимая валидация физической модели.
Дополнительно проверены автоматическая остановка при бесконечном dt,
остановка по заданному лимиту шагов и создание рисунка в обычном режиме.
